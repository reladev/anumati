apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'signing'
apply plugin: 'idea'

archivesBaseName = 'anumati-hibernate'
group = 'org.reladev.anumati'
version = '0.1-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile project(":core")
    compile group: 'org.hibernate', name: 'hibernate-core', version: '5.2.8.Final'
    compile group: 'org.hibernate', name: 'hibernate-entitymanager', version: '5.2.8.Final'
    testCompile group: 'junit', name: 'junit', version: '4.10'
    testCompile group: 'com.h2database', name: 'h2', version: '1.4.193'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

jar {
   from sourceSets.main.allJava
}

javadoc {
    options.addStringOption("sourcepath", "src/")
}

task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from 'build/docs/javadoc'
}
 
task sourcesJar(type: Jar) {
    from sourceSets.main.allSource
    classifier = 'sources'
}

uploadArchives {
    repositories {
        mavenDeployer {beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment)}

            repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
                authentication(userName: sonatypeUsername, password: sonatypePassword)
            }
            snapshotRepository(url: "file://localhost/Users/jeffg/.m2/repository")
//            snapshotRepository(url: "https://oss.sonatype.org/content/repositories/snapshots/") {
//                authentication(userName: sonatypeUsername, password: sonatypePassword)
//            }

            pom.project {
               name 'Anumati Security'
               artifactId 'anumati-hibernate'
               packaging 'jar'
               description 'Hibernate extensions for Anumati framework'
               url 'https://github.com/reladev/anumati'

               scm {
                   url 'scm:git:https://github.com/reladev/anumati.git'
                   connection 'scm:git:https://github.com/reladev/anumati.git'
                   developerConnection 'scm:git:https://github.com/reladev/anumati.git'
               }

               licenses {
                   license {
                       name 'The Apache Software License, Version 2.0'
                       url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                       distribution 'repo'
                   }
               }

               developers {
                   developer {
                       id 'jeffgnpc'
                       name 'Jeffrey Kleiss'
                   }
               }
           }
        }
    }
}




